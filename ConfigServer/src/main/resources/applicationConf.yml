server:
  port: 8888

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alpadev44/config_server_parcial.git
          default-label: main
